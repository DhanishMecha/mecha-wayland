[package]
name = "widgets"
version = "0.1.0"
edition = "2024"
build = "build.rs"

[dependencies]
app = { workspace = true }
assets = { workspace = true }
layout = { workspace = true }
paint = { workspace = true }
theme = { workspace = true }
utils = { workspace = true }

[build-dependencies]
assets = { workspace = true, features = ["builder"] }